#
# Gradle 데몬을 돌릴 JVM 조건.
#
# Gradle 9 부터 데몬은 JVM 17 이상을 요구한다. 셸의 기본 JDK 가 11 이어도
# 이 파일이 있으면 Gradle 이 설치된 JDK 중 17 을 찾아 데몬을 띄우므로,
# 개발자마다 JAVA_HOME 을 따로 맞출 필요가 없다.
#
# (`./gradlew updateDaemonJvm --jvm-version=17` 로도 생성할 수 있다)
#
toolchainVersion=17
