spring:
  datasource:
    url: jdbc:h2:file:./data/boarddb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  h2:
    console:
      # 기본값을 꺼 둔다. 켜져 있으면 DB 를 브라우저에서 그대로 열 수 있으므로,
      # 운영에서 끄는 것을 "잊지 않아야" 하는 구조는 언젠가 잊는다.
      # 로컬에서 필요할 때만 H2_CONSOLE_ENABLED=true 로 켠다.
      enabled: ${H2_CONSOLE_ENABLED:false}
      path: /h2-console
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

file:
  upload-dir: ${user.home}/board-uploads/

app:
  # 비밀번호 재설정 메일에 담을 링크의 앞부분 - 배포 주소에 맞춰 바꾼다
  base-url: ${APP_BASE_URL:http://localhost:8080}

mail:
  # log = 실제로 보내지 않고 콘솔에 출력 (SMTP 계정 없이 개발할 수 있다)
  # 운영에서는 smtp 로 두고 MailSender 구현체를 등록한다
  mode: ${MAIL_MODE:log}

jwt:
  # 로컬 개발용 시크릿 - 운영 환경에서는 반드시 환경변수(JWT_SECRET)로 교체할 것 (HS256, 32바이트 이상)
  secret: ${JWT_SECRET:study-board-local-dev-secret-key-please-override}
  # 액세스 토큰은 무상태라 폐기할 수 없으므로 짧게, 재로그인은 리프레시 토큰으로 대신한다
  access-token-minutes: 15
  refresh-token-days: 14
